#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Executando verificações pré-commit..."

# Executa lint-staged (Biome lint + format)
npx lint-staged

# Verifica tipos TypeScript
echo "🔍 Verificando tipos TypeScript..."
npm run type-check

echo "✅ Verificações pré-commit concluídas!"